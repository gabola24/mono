services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: zukuri
      POSTGRES_PASSWORD: zukuri
      POSTGRES_DB: zukuri
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zukuri"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
